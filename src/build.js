// Bumped on every push, so it's obvious which version is actually running.
export const BUILD = { number: 32, date: '2026-09-12' };
export const BUILD_LABEL = `build ${BUILD.number} · ${BUILD.date}`;
